# MySQL

MySQL is an open-source relational database. Its default storage engine, InnoDB,
uses row-level locking combined with multi-versioning via undo logs. Readers see
a consistent snapshot at the time their transaction started.

See also: [Postgres](entities/postgres.md), [Postgres vs MySQL concurrency](comparisons/postgres-vs-mysql-concurrency.md).